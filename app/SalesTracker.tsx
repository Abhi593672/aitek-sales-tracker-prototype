// @ts-nocheck
"use client";
import {FormEvent,useEffect,useState} from "react";
import {Activity,activitySubjects,activityTypes,businessUnits,currencies,decisionLevels,interactionModes,leadSources,lostReasons,nextActions,Opportunity,opportunityTypes,QuoteRequest,Reseller,riskLevels,riskTypes,Role,seedActivities,seedOpportunities,seedQuotes,seedResellers,stageProbability,stages,users,vendors} from "@/src/data/seed";
import {ActivityFormV2,CloseOpportunityV2,OpportunityDetailV2,OpportunityEditV2,OpportunityFormV2} from "./OpportunityWorkspace";

type Store={resellers:Reseller[];opportunities:Opportunity[];activities:Activity[];quotes:QuoteRequest[];notifications:string[]};
const initial:Store={resellers:seedResellers,opportunities:seedOpportunities,activities:seedActivities,quotes:seedQuotes,notifications:["QT-820 v2 was published for ABC Technologies"]};
const nav:Record<Role,string[]>={KAM:["Dashboard","My Strategic Resellers","Opportunities","Activities","Quotations","Orders","Notifications","Reports"],IST:["Dashboard","Quotation Request Pool","My Assigned Requests","Reseller Onboarding","Quotations","Orders","Notifications"],DESK_MANAGER:["Dashboard","IST Pool","SLA Monitor","Team Workload","Blocked Requests","Notifications","Reports"],REGIONAL_MANAGER:["Dashboard","Regional Pipeline","KAM Performance","Reseller Performance","Quotations","Orders","Won / Lost Analysis","Reports"]};
const loginAccounts=[
 {email:"aminata.kone@aitek.com",password:"KAM@2026",role:"KAM" as Role,scope:"DR CI & WEST · AITEK Ivory Coast"},
 {email:"yamin.shaikh@aitek.com",password:"IST@2026",role:"IST" as Role,scope:"Central Inside Sales · All regions and entities"},
 {email:"nadia.mensah@aitek.com",password:"DESK@2026",role:"DESK_MANAGER" as Role,scope:"Central IST Desk · All regions and entities"},
 {email:"koffi.traore@aitek.com",password:"REGION@2026",role:"REGIONAL_MANAGER" as Role,scope:"DR CI & WEST · Regional Management"},
];
const istTeam=["Yamin Shaikh","Fatou Diop","Ibrahim Koné","Mariam Diallo","Jean Kouassi"];
const money=(v?:number)=>v?`€${v.toLocaleString()}`:"—";
const tenderClassificationTriggers=["Tender / RFP / RFQ / formal procurement","Tender reference number","Formal submission deadline","Project-based opportunity","Multiple lots or structured deliverables","Technical + financial proposal","Compliance, legal documents or bid bond","Strategic-value threshold","Large project pipeline opportunity"];
const complexClassificationTriggers=["Multiple Business Units","Multiple vendors","Technical / architecture validation","Pre-Sales involvement","Services: install, deploy, integrate, migrate, train, support or manage","Special pricing or vendor negotiation","Margin, risk or management approval","Non-standard / custom products","Initial request requires clarification","Complexity-value threshold","Hardware + software + services combination"];
const classificationLabel=(v?:string)=>v==="Tender"?"Tender / Project":v||"Awaiting classification";
function deriveClassification(q:QuoteRequest,o?:Opportunity){const text=`${q.products} ${o?.description||""} ${o?.opportunityType||""} ${o?.source||""}`.toLowerCase();const triggers:string[]=o?.projectBased?["Project-based opportunity"]:[];if(/tender|rfp|rfq|appel d.offres|bid|formal procurement/.test(text))triggers.push("Tender / RFP / RFQ / formal procurement");if(/tender reference|reference number/.test(text))triggers.push("Tender reference number");if(/submission deadline|bid deadline/.test(text))triggers.push("Formal submission deadline");if(/project|datacenter|data center|transformation/.test(text)&&!triggers.includes("Project-based opportunity"))triggers.push("Project-based opportunity");if(/multiple lot|lots|phase|deliverable/.test(text))triggers.push("Multiple lots or structured deliverables");if(/technical proposal|financial proposal/.test(text))triggers.push("Technical + financial proposal");if(/compliance|certificate|bid bond|legal document/.test(text))triggers.push("Compliance, legal documents or bid bond");if((o?.amount||0)>=500000)triggers.push("Strategic-value threshold");if(/large project|national infrastructure|government/.test(text))triggers.push("Large project pipeline opportunity");if(triggers.length)return {classification:"Tender" as const,triggers};if(/multi.?bu|multiple business unit/.test(text))triggers.push("Multiple Business Units");if(/multi.?vendor|multiple vendor/.test(text))triggers.push("Multiple vendors");if(/technical|architecture|sizing|specification/.test(text))triggers.push("Technical / architecture validation");if(/pre.?sales/.test(text))triggers.push("Pre-Sales involvement");if(/install|deploy|integration|migration|training|support|managed service/.test(text))triggers.push("Services: install, deploy, integrate, migrate, train, support or manage");if(/special price|discount|vendor negotiation/.test(text))triggers.push("Special pricing or vendor negotiation");if(/approval|margin|risk review/.test(text))triggers.push("Margin, risk or management approval");if(/custom|non-standard|z-article/.test(text))triggers.push("Non-standard / custom products");if(/clarif|confirm|incomplete|to be confirmed/.test(text))triggers.push("Initial request requires clarification");if((o?.amount||0)>=200000)triggers.push("Complexity-value threshold");if(/hardware.*software|software.*service|hardware.*service/.test(text))triggers.push("Hardware + software + services combination");return {classification:(triggers.length?"Complex":"Standard") as "Complex"|"Standard",triggers};}
const quoteSlaLabel=(q:QuoteRequest)=>q.classification==="Standard"?`Issue within 2h${q.quoteSlaDueAt?` · ${new Date(q.quoteSlaDueAt).toLocaleString()}`:""}`:q.classification==="Complex"?`Qualify within 2h · Deliver ${q.targetDeliveryAt?new Date(q.targetDeliveryAt).toLocaleString():"target required"}`:q.classification==="Tender"?`Governance · Deadline ${q.tenderSubmissionDeadline?new Date(q.tenderSubmissionDeadline).toLocaleString():"required"}`:"Starts after IST classification";
const isSlaBreached=(q:QuoteRequest)=>q.pickupSlaMinutes<0||!!(q.quoteSlaDueAt&&new Date(q.quoteSlaDueAt).getTime()<Date.now()&&!['Published','Accepted'].includes(q.status))||!!(q.qualificationDueAt&&new Date(q.qualificationDueAt).getTime()<Date.now()&&!q.targetDeliveryAt);
const daysFrom=(date?:string)=>date?Math.max(0,Math.floor((new Date("2026-08-19").getTime()-new Date(date).getTime())/86400000)):undefined;
const daysUntil=(date?:string)=>date?Math.floor((new Date(date).getTime()-new Date("2026-08-19").getTime())/86400000):undefined;
const delayStatus=(o:Opportunity)=>!o.expectedCloseDate?"No closing date":(daysUntil(o.expectedCloseDate)!<0&&o.status==="Active"?"Late":"On track");
const actionMissing=(o:Opportunity)=>o.status==="Active"&&(!o.nextAction||!o.nextDate||!o.nextActionDescription);

export default function SalesTracker(){
 const [role,setRole]=useState<Role|null>(null); const [section,setSection]=useState("Dashboard");
 const [store,setStore]=useState<Store>(initial); const [modal,setModal]=useState<null|"opportunity"|"quote"|"newreseller"|"activity"|"edit"|"close">(null);
 const [selectedOpp,setSelectedOpp]=useState<string|null>(null); const [toast,setToast]=useState("");
 useEffect(()=>{const raw=localStorage.getItem("aitek-demo-store");if(raw)try{const saved=JSON.parse(raw);const existing=saved.quotes||[];const merged=[...existing,...seedQuotes.filter(seed=>!existing.some((q:QuoteRequest)=>q.id===seed.id))];const quotes=merged.map((q:QuoteRequest)=>({...q,source:q.source||"KAM",receivedAt:q.receivedAt||"2026-08-18T09:00:00",pickupSlaMinutes:Number.isFinite(q.pickupSlaMinutes)?q.pickupSlaMinutes:30}));setStore({...initial,...saved,quotes,activities:saved.activities||seedActivities})}catch{}},[]);
 useEffect(()=>{const saved=sessionStorage.getItem("aitek-auth-role") as Role|null;if(saved&&users[saved])setRole(saved)},[]);
 useEffect(()=>{localStorage.setItem("aitek-demo-store",JSON.stringify(store))},[store]);
 const login=(r:Role)=>{sessionStorage.setItem("aitek-auth-role",r);setRole(r);setSection("Dashboard")}; const logout=()=>{sessionStorage.removeItem("aitek-auth-role");setRole(null);setSection("Dashboard")}; const notify=(m:string)=>{setToast(m);setTimeout(()=>setToast(""),2600)};
 const reset=()=>{setStore(initial);localStorage.removeItem("aitek-demo-store");notify("Demo data reset")};
 if(!role)return <Login onLogin={login}/>;
 const user=users[role];
 const account=loginAccounts.find(a=>a.role===role)!;
 return <div className="app"><aside><div className="brand"><span>A</span><b>AITEK</b><small>Sales Tracker</small></div><div className="role">{user.title}</div>{nav[role].map(n=><button key={n} className={section===n?"active":""} onClick={()=>setSection(n)}>{n}</button>)}<div className="asideFoot"><button onClick={reset}>Reset demo</button><button onClick={logout}>Logout</button></div></aside><main><header><div><small>{account.scope}</small><h1>{section}</h1></div><div className="user"><b>{user.name}</b><span>{role.replaceAll("_"," ")}</span></div></header><div className="content">{role==="KAM"&&<Kam section={section} store={store} setStore={setStore} modal={modal} setModal={setModal} selectedOpp={selectedOpp} setSelectedOpp={setSelectedOpp} notify={notify}/>} 
 {role==="IST"&&<Ist section={section} store={store} setStore={setStore} notify={notify}/>} 
 {role==="DESK_MANAGER"&&<Desk section={section} store={store} setStore={setStore} notify={notify}/>} 
 {role==="REGIONAL_MANAGER"&&<Regional section={section} store={store}/>} 
 </div></main>{toast&&<div className="toast">✓ {toast}</div>}</div>
}

function Login({onLogin}:{onLogin:(r:Role)=>void}){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const submit=(e:FormEvent)=>{e.preventDefault();const account=loginAccounts.find(a=>a.email.toLowerCase()===email.trim().toLowerCase()&&a.password===password);if(!account){setError("Email address or password is incorrect.");return}setError("");onLogin(account.role)};return <div className="login"><div className="loginCard authCard"><div className="logo"><span>A</span><div><b>AITEK</b><small>Sales Tracker</small></div></div><h1>Sign in</h1><p>Use your assigned AITEK account. Your role, menu and data scope are resolved automatically after authentication.</p><form className="authForm" onSubmit={submit}><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@aitek.com" autoComplete="username" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter password" autoComplete="current-password" required/></label>{error&&<div className="authError">{error}</div>}<button className="primary" type="submit">Sign in</button></form><div className="demoAccounts"><b>Prototype accounts</b>{loginAccounts.map(a=><button type="button" key={a.role} onClick={()=>{setEmail(a.email);setPassword(a.password);setError("")}}><span>{users[a.role].title}</span><small>{a.email} · {a.password}</small></button>)}</div><div className="scope"><b>Access model</b><span>KAM and Regional Manager are region-scoped. IST and Desk Manager are central operational roles with access across regions and entities.</span></div></div></div>}

type Common={store:Store;setStore:(s:Store)=>void;notify:(s:string)=>void};
function Kam({section,store,setStore,modal,setModal,selectedOpp,setSelectedOpp,notify}:Common&{section:string;modal:string|null;setModal:(m:any)=>void;selectedOpp:string|null;setSelectedOpp:(s:string|null)=>void}){
 const [selectedReseller,setSelectedReseller]=useState<string|null>(null); const [selectedQuote,setSelectedQuote]=useState<string|null>(null); const myOpportunities=store.opportunities.filter(o=>o.owner===users.KAM.name); const active=myOpportunities.filter(o=>o.status==="Active"); const myResellerIds=new Set(store.resellers.filter(r=>r.type==="Strategic"&&r.kam===users.KAM.name).map(r=>r.id)); const myQuotes=store.quotes.filter(q=>myResellerIds.has(q.resellerId)||myOpportunities.some(o=>o.id===q.opportunityId)); const pipeline=active.reduce((a,o)=>a+(o.amount||0),0); const selected=myOpportunities.find(o=>o.id===selectedOpp); const reseller=store.resellers.find(r=>r.id===selectedReseller); const quoteForPayment=myQuotes.find(q=>q.id===selectedQuote);
 if(section==="Dashboard")return <><div className="cards"><Card t="Active Opportunities" v={active.length}/><Card t="Open Pipeline" v={money(pipeline)}/><Card t="Weighted Pipeline" v={money(active.reduce((a,o)=>a+o.weightedPipeline,0))}/><Card t="Next Actions Due" v={active.filter(o=>o.nextDate&&o.nextDate<="2026-08-18").length}/><Card t="Quote Mix S / C / T" v={`${store.quotes.filter(q=>q.classification==="Standard").length} / ${store.quotes.filter(q=>q.classification==="Complex").length} / ${store.quotes.filter(q=>q.classification==="Tender").length}`}/></div><Panel title="Priority Opportunities"><OpportunityTable rows={store.opportunities} onOpen={setSelectedOpp}/></Panel>{selected&&<OpportunityDetailV2 o={selected} store={store} setModal={setModal}/>} {modal==="quote"&&selected&&<QuoteForm store={store} setStore={setStore} close={()=>setModal(null)} notify={notify} preselectedOpportunity={selected.id}/>} {modal==="activity"&&selected&&<ActivityFormV2 opportunity={selected} store={store} setStore={setStore} close={()=>setModal(null)} notify={notify}/>} {modal==="edit"&&selected&&<OpportunityEditV2 opportunity={selected} store={store} setStore={setStore} close={()=>setModal(null)} notify={notify}/>} {modal==="close"&&selected&&<CloseOpportunityV2 opportunity={selected} store={store} setStore={setStore} close={()=>setModal(null)} notify={notify}/>}</>;
 if(section==="My Strategic Resellers")return reseller?<><ResellerDetail reseller={reseller} store={store} back={()=>setSelectedReseller(null)} createOpportunity={()=>setModal("opportunity")} notify={notify}/>{modal==="opportunity"&&<OpportunityFormV2 store={store} setStore={setStore} close={()=>setModal(null)} notify={notify} preselectedResellerId={reseller.id}/>}</>:<Panel title="My Strategic Resellers"><p className="hint">Read-only list of Strategic Resellers assigned to the logged-in KAM. Reseller creation and assignment are managed outside this screen.</p><table><thead><tr><th>Reseller</th><th>Country</th><th>Primary contact</th><th>Status</th><th>Open opportunities</th><th>Open quote requests</th><th>Action</th></tr></thead><tbody>{store.resellers.filter(r=>r.type==="Strategic"&&r.kam===users.KAM.name).map(r=><tr key={r.id}><td><b>{r.name}</b><small>{r.id}</small></td><td>{r.country}</td><td>{r.contact||"Not available"}<small>{r.email||"—"}</small></td><td><Status s={r.status}/></td><td>{store.opportunities.filter(o=>o.resellerId===r.id&&o.status==="Active").length}</td><td>{store.quotes.filter(q=>q.resellerId===r.id&&!['Published','Accepted'].includes(q.status)).length}</td><td><button className="primary" onClick={()=>setSelectedReseller(r.id)}>Open</button></td></tr>)}</tbody></table></Panel>;
 if(section==="Opportunities")return <><div className="pageActions"><button className="primary" onClick={()=>setModal("opportunity")}>+ Create Opportunity</button></div><Panel title="My Opportunities"><OpportunityTable rows={store.opportunities} onOpen={setSelectedOpp}/></Panel>{selected&&<OpportunityDetailV2 o={selected} store={store} setModal={setModal}/>} {modal==="opportunity"&&<OpportunityFormV2 store={store} setStore={setStore} close={()=>setModal(null)} notify={notify}/>} {modal==="quote"&&<QuoteForm store={store} setStore={setStore} close={()=>setModal(null)} notify={notify} preselectedOpportunity={selected?.id}/>} {modal==="activity"&&selected&&<ActivityFormV2 opportunity={selected} store={store} setStore={setStore} close={()=>setModal(null)} notify={notify}/>} {modal==="edit"&&selected&&<OpportunityEditV2 opportunity={selected} store={store} setStore={setStore} close={()=>setModal(null)} notify={notify}/>} {modal==="close"&&selected&&<CloseOpportunityV2 opportunity={selected} store={store} setStore={setStore} close={()=>setModal(null)} notify={notify}/>}</>;
 if(section==="Quotations")return <><div className="pageActions"><button className="primary" onClick={()=>setModal("quote")}>+ Request Quotation</button></div><KamQuoteTable store={store} rows={myQuotes} onOffline={(id)=>{setSelectedQuote(id);setModal("offlinePayment")}}/>{modal==="quote"&&<QuoteForm store={store} setStore={setStore} close={()=>setModal(null)} notify={notify}/>} {modal==="offlinePayment"&&quoteForPayment&&<OfflinePaymentForm q={quoteForPayment} store={store} setStore={setStore} close={()=>setModal(null)} notify={notify}/>}</>;
 if(section==="Orders")return <Panel title="My Strategic Reseller Orders"><p className="hint">KAM can track order, payment proof and finance status for assigned strategic resellers. KAM does not place orders by default; authorised IST or Desk Manager completes order placement when offline acceptance/payment is received.</p><table><thead><tr><th>Quote / Order Source</th><th>Reseller</th><th>Product</th><th>Commercial Status</th><th>Payment Proof</th><th>Operational Owner</th></tr></thead><tbody>{myQuotes.filter(q=>q.status==="Published"||q.offlineAcceptance||q.orderOnBehalf).map(q=>{const r=store.resellers.find(x=>x.id===q.resellerId);return <tr key={q.id}><td><b>{q.id}</b><small>{q.opportunityId}</small></td><td>{r?.name}</td><td>{q.products}</td><td><Status s={q.orderOnBehalf?"Order Placed":q.offlineAcceptance?"Pending IST Order Placement":q.status}/></td><td>{q.offlineAcceptance?<>{q.offlineAcceptance.proofFileName}<small>{q.offlineAcceptance.paymentReference}</small></>:"Not received"}</td><td>{q.assignedTo||"IST to assign"}<small>{q.orderOnBehalf?`Placed by ${q.orderOnBehalf.placedBy}`:"KAM visibility only"}</small></td></tr>})}</tbody></table></Panel>;
 if(section==="Activities")return <><Panel title="My Activities & Next Actions"><table><thead><tr><th>Date</th><th>Opportunity</th><th>Activity</th><th>Mode</th><th>Contact / Level</th><th>Subject</th><th>Duration</th></tr></thead><tbody>{store.activities.map(a=><tr key={a.id}><td>{a.date}</td><td>{a.opportunityId}</td><td>{a.activityType}</td><td>{a.interactionMode}</td><td>{a.contact}<small>{a.decisionLevel}</small></td><td>{a.subject}</td><td>{a.duration} h</td></tr>)}</tbody></table></Panel><Panel title="Upcoming Actions"><table><thead><tr><th>Opportunity</th><th>Next action</th><th>Description</th><th>Date</th><th>Status</th></tr></thead><tbody>{store.opportunities.filter(o=>o.status==="Active").map(o=><tr key={o.id}><td>{o.id} · {o.name}</td><td>{o.nextAction}</td><td>{o.nextActionDescription}</td><td>{o.nextDate}</td><td><Status s={o.nextDate<"2026-08-18"?"Overdue":"Open"}/></td></tr>)}</tbody></table></Panel></>;
 if(section==="Notifications")return <Panel title="Notifications">{store.notifications.map((n,i)=><div className="notice" key={i}>{n}</div>)}</Panel>;
 return <Placeholder section={section}/>;
}

function ResellerDetail({reseller,store,back,createOpportunity,notify}:{reseller:Reseller;store:Store;back:()=>void;createOpportunity:()=>void;notify:(s:string)=>void}){const opportunities=store.opportunities.filter(o=>o.resellerId===reseller.id);const quotes=store.quotes.filter(q=>q.resellerId===reseller.id);return <Panel title={`${reseller.name} · ${reseller.id}`}><div className="pageActions"><Status s={`${reseller.type} · ${reseller.status}`}/><button className="primary" onClick={createOpportunity}>Create Opportunity</button><button onClick={()=>notify("Activity form will open with this reseller preselected")}>Record Activity</button><button onClick={()=>notify(`${quotes.length} linked quotation records`) }>View Quotations</button><button onClick={()=>notify("Orders filtered to this reseller")}>View Orders</button><button onClick={back}>Back to My Resellers</button></div><div className="detailGrid expanded"><Info t="Reseller Information" lines={[`Country: ${reseller.country}`,"Region: DR CI & WEST","Entity: AITEK Ivory Coast",`Assigned KAM: ${reseller.kam}`]}/><Info t="Primary Contact" lines={[reseller.contact||"Not available",reseller.email||"—",reseller.phone||"—"]}/><Info t="Commercial Summary" lines={[`${opportunities.filter(o=>o.status==="Active").length} active Opportunities`,`${quotes.filter(q=>!['Published','Accepted'].includes(q.status)).length} open Quote Requests`,`${quotes.filter(q=>q.status==="Published").length} published Quotations`]}/><Info t="Permission" lines={["Read-only reseller master data","Opportunity and Activity creation permitted","Only records within current KAM assignment scope"]}/></div><h3>Open Opportunities</h3><OpportunityTable rows={opportunities} onOpen={()=>notify("Open the selected Opportunity from the Opportunities module")}/><h3>Recent Quote Requests and Quotations</h3><table><thead><tr><th>Request</th><th>Source</th><th>Product</th><th>Classification</th><th>Status</th><th>Version</th></tr></thead><tbody>{quotes.map(q=><tr key={q.id}><td>{q.id}</td><td>{q.source}</td><td>{q.products}</td><td>{q.classification||"IST to classify"}</td><td><Status s={q.status}/></td><td>v{q.version}</td></tr>)}</tbody></table></Panel>}

function OpportunityForm({store,setStore,close,notify}:Common&{close:()=>void}){const [accountType,setAccountType]=useState("Strategic"); const [name,setName]=useState(""); const [reseller,setReseller]=useState(store.resellers[0]?.id||""); const [newName,setNewName]=useState(""); const [draft,setDraft]=useState(false); const submit=(e:FormEvent)=>{e.preventDefault();if(!name||(!reseller&&accountType==="Strategic")||(!newName&&accountType==="New"))return;let rs=[...store.resellers],rid=reseller;if(accountType==="New"){rid=`NEW-${Date.now().toString().slice(-5)}`;rs.push({id:rid,name:newName,type:"New",country:"Côte d’Ivoire",status:"Onboarding",kam:"Aminata Koné"})}const o:Opportunity={id:`OPP-2026-${String(store.opportunities.length+153).padStart(4,"0")}`,name,resellerId:rid,owner:"Aminata Koné",stage:draft?"":"Identified",nextAction:draft?"":"Customer qualification",nextDate:draft?"":"2026-08-25",status:draft?"Draft":"Active"};setStore({...store,resellers:rs,opportunities:[o,...store.opportunities],notifications:[`${o.id} created by KAM`,...store.notifications]});notify(draft?"Opportunity saved as Draft":"Opportunity created");close()};return <Modal title="Create Opportunity" close={close}><form onSubmit={submit} className="form"><label>Opportunity Name *<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Account Type *<select value={accountType} onChange={e=>setAccountType(e.target.value)}><option value="Strategic">Assigned Strategic Reseller</option><option value="New">New Reseller</option></select></label>{accountType==="Strategic"?<label>Strategic Reseller *<select value={reseller} onChange={e=>setReseller(e.target.value)}>{store.resellers.filter(r=>r.type==="Strategic").map(r=><option value={r.id} key={r.id}>{r.name}</option>)}</select></label>:<><label>New Reseller Name *<input value={newName} onChange={e=>setNewName(e.target.value)} required/></label><label>Country *<select><option>Côte d’Ivoire</option><option>Senegal</option></select></label></>}<label>Opportunity Type *<select>{opportunityTypes.map(x=><option key={x}>{x}</option>)}</select></label><label>Business Unit *<select><option>Infra & Cloud</option><option>Cyber</option><option>Energy</option><option>Device & Print</option></select></label><label>Primary Vendor *<select><option>Microsoft</option><option>HP</option><option>APC</option><option>Dell</option></select></label><label>Next Action *<select>{nextActions.map(x=><option key={x}>{x}</option>)}</select></label><label>Next Action Date *<input type="date" defaultValue="2026-08-25"/></label><label className="full">Opportunity Description *<textarea required defaultValue="Commercial opportunity identified by KAM."/></label><div className="formActions full"><button type="button" onClick={close}>Cancel</button><button type="submit" onClick={()=>setDraft(true)}>Save Draft</button><button className="primary" type="submit" onClick={()=>setDraft(false)}>Create Opportunity</button></div></form></Modal>}

function OpportunityDetail({o,store,setStore,setModal,notify}:Common&{o:Opportunity;setModal:(m:any)=>void}){const r=store.resellers.find(x=>x.id===o.resellerId);return <Panel title={`${o.id} · ${o.name}`}><div className="pageActions"><Status s={o.status}/><button onClick={()=>{const stage=stages[Math.min(stages.indexOf(o.stage)+1,stages.length-1)];setStore({...store,opportunities:store.opportunities.map(x=>x.id===o.id?{...x,stage}:x)});notify(`Stage updated to ${stage}`)}}>Advance stage</button><button className="primary" disabled={o.status==="Draft"} onClick={()=>setModal("quote")}>Request Quotation</button></div><div className="detailGrid"><div><small>Account</small><b>{r?.name}</b></div><div><small>Stage</small><b>{o.stage||"Not started"}</b></div><div><small>Amount</small><b>{money(o.amount)}</b></div><div><small>Next Action</small><b>{o.nextAction||"Not set"} {o.nextDate}</b></div></div></Panel>}

function QuoteForm({store,setStore,close,notify,preselectedOpportunity}:Common&{close:()=>void;preselectedOpportunity?:string}){
 const preselected=store.opportunities.find(o=>o.id===preselectedOpportunity);const strategic=store.resellers.filter(r=>r.type==="Strategic"&&r.kam===users.KAM.name);const [resellerId,setResellerId]=useState(preselected?.resellerId||strategic[0]?.id||"");const eligible=store.opportunities.filter(o=>o.status==="Active"&&o.resellerId===resellerId);const [opp,setOpp]=useState(preselectedOpportunity||eligible[0]?.id||"__new__");const [product,setProduct]=useState("Microsoft 365 Business Premium");const [quantity,setQuantity]=useState(10);const [currency,setCurrency]=useState("EUR");const [delivery,setDelivery]=useState("Standard delivery conditions");const [notes,setNotes]=useState("");
 const changeReseller=(id:string)=>{setResellerId(id);const first=store.opportunities.find(o=>o.status==="Active"&&o.resellerId===id);setOpp(first?.id||"__new__")};
 const submit=(e:FormEvent)=>{e.preventDefault();const r=store.resellers.find(x=>x.id===resellerId);if(!r){notify("Select an assigned Strategic Reseller");return}if(!product.trim()||quantity<1){notify("Product and a valid Quantity are required");return}let opportunities=[...store.opportunities];let opportunity=store.opportunities.find(x=>x.id===opp);if(opp==="__new__"){const probability=stageProbability["Qualification"];opportunity={id:`OPP-2026-${String(153+store.opportunities.length).padStart(4,"0")}`,name:`Direct Quote Request · ${r.name} · ${product}`,resellerId:r.id,owner:users.KAM.name,regionalDirector:"Koffi Traoré",country:r.country,source:"Inbound",businessUnit:"To be confirmed",vendor:"To be confirmed",description:`Opportunity generated from direct KAM Quote Request for ${product}. ${notes}`.trim(),opportunityType:"New",stage:"Qualification",probability,weightedPipeline:0,createdDate:"2026-08-18",nextAction:"Customer follow-up",nextDate:"2026-08-20",nextActionDescription:"Follow up after quotation preparation",risk:"Not assessed",status:"Active"};opportunities=[opportunity,...opportunities]}else if(!opportunity){notify("Select an existing Opportunity or Create Opportunity and Continue");return}else{opportunities=opportunities.map(x=>x.id===opportunity!.id?{...x,stage:"Qualification",probability:stageProbability["Qualification"],weightedPipeline:(x.amount||0)*stageProbability["Qualification"]/100,nextAction:"Customer follow-up",nextActionDescription:"Follow up after quotation preparation"}:x)}const q:QuoteRequest={id:`QR-2026-${String(store.quotes.length+821).padStart(5,"0")}`,opportunityId:opportunity.id,resellerId:r.id,products:product,quantity,status:"In IST Pool",source:"KAM",receivedAt:new Date().toISOString(),pickupSlaMinutes:30,blocked:r.status==="Onboarding"?"Reseller must be created and linked in AITEKCenter":undefined,version:0};setStore({...store,quotes:[q,...store.quotes],opportunities,notifications:[`${q.id} submitted to IST Pool from KAM Quotation module · ${delivery}`,...store.notifications]});notify(opp==="__new__"?`Opportunity ${opportunity.id} created and Quote Request sent to IST Pool`:"Quote Request linked to Opportunity and sent to IST Pool");close()};
 return <Modal title="Request Quotation" close={close}><form onSubmit={submit} className="form"><div className="full formSection"><b>Direct KAM quotation entry</b><span>Every Quote Request must be linked to exactly one Opportunity.</span></div><label>Assigned Strategic Reseller *<select value={resellerId} disabled={!!preselectedOpportunity} onChange={e=>changeReseller(e.target.value)}>{strategic.map(r=><option value={r.id} key={r.id}>{r.name} · {r.country}</option>)}</select></label><label>Opportunity handling *<select value={opp} disabled={!!preselectedOpportunity} onChange={e=>setOpp(e.target.value)}>{eligible.map(o=><option value={o.id} key={o.id}>{o.id} · {o.name}</option>)}<option value="__new__">Create Opportunity and Continue</option></select></label>{opp==="__new__"&&<p className="full hint"><b>System action:</b> Create a controlled Opportunity owned by the logged-in KAM at Qualification stage, then link this Quote Request to it.</p>}<label>Product *<input value={product} onChange={e=>setProduct(e.target.value)}/></label><label>Quantity *<input type="number" min="1" value={quantity} onChange={e=>setQuantity(Number(e.target.value))}/></label><label>Currency *<select value={currency} onChange={e=>setCurrency(e.target.value)}><option>EUR</option><option>AED</option><option>MAD</option><option>USD</option><option>XOF</option></select></label><label>Delivery Conditions *<textarea value={delivery} onChange={e=>setDelivery(e.target.value)}/></label><label className="full">Additional Product / Commercial Notes<textarea value={notes} onChange={e=>setNotes(e.target.value)}/></label><p className="full hint"><b>Classification boundary:</b> KAM does not select Standard, Complex or Tender. The 30-minute Pickup SLA starts when submitted; the Quotation SLA starts after IST pickup, review and classification.</p><div className="formActions full"><button type="button" onClick={close}>Cancel</button><button className="primary">Submit Quote Request</button></div></form></Modal>
}

function Ist({section,store,setStore,notify}:Common&{section:string}){const [selectedRequest,setSelectedRequest]=useState<string|null>(null);const [prepareRequest,setPrepareRequest]=useState<string|null>(null);const [sourceFilter,setSourceFilter]=useState("All");const pool=store.quotes.filter(q=>q.status==="In IST Pool");const assigned=store.quotes.filter(q=>q.assignedTo==="Yamin Shaikh"&&!['Published','Accepted','Order Placed'].includes(q.status));const orderQueue=store.quotes.filter(q=>q.offlineAcceptance&&!q.orderOnBehalf);const update=(id:string,patch:Partial<QuoteRequest>,msg:string)=>{setStore({...store,quotes:store.quotes.map(q=>q.id===id?{...q,...patch}:q),notifications:[msg,...store.notifications]});notify(msg)};const selected=store.quotes.find(q=>q.id===selectedRequest);const preparing=store.quotes.find(q=>q.id===prepareRequest);if(preparing)return <QuotationPreparation q={preparing} store={store} update={update} back={()=>setPrepareRequest(null)} notify={notify}/>;if(section==="Dashboard")return <><div className="cards"><Card t="Pool" v={pool.length}/><Card t="My Assigned" v={assigned.length}/><Card t="Pickup SLA Breached" v={pool.filter(q=>q.pickupSlaMinutes<0).length}/><Card t="Offline Payment Handoffs" v={orderQueue.length}/><Card t="Quote Mix S / C / T" v={`${store.quotes.filter(q=>q.classification==="Standard").length} / ${store.quotes.filter(q=>q.classification==="Complex").length} / ${store.quotes.filter(q=>q.classification==="Tender").length}`}/></div><Panel title="Work requiring attention"><QuoteWork rows={assigned} update={update} onOpen={setSelectedRequest}/></Panel></>;if(section==="Reseller Onboarding")return <Panel title="New Reseller Onboarding">{store.resellers.filter(r=>r.status==="Onboarding").map(r=><div className="work" key={r.id}><div><b>{r.name}</b><small>{r.id} · New Reseller · quotation publication blocked</small></div><button className="primary" onClick={()=>{setStore({...store,resellers:store.resellers.map(x=>x.id===r.id?{...x,status:"Active"}:x),quotes:store.quotes.map(q=>q.resellerId===r.id?{...q,blocked:undefined}:q)});notify(`${r.name} created and linked`)}}>Mark created & linked</button></div>)}</Panel>;if(section==="Quotation Request Pool")return <><Panel title="Quotation Request Pool"><div className="pageActions"><label>Source <select value={sourceFilter} onChange={e=>setSourceFilter(e.target.value)}><option>All</option><option>KAM</option><option>AITEKCenter</option><option>Outlook / Email</option></select></label></div><QuotePoolTable rows={pool.filter(q=>sourceFilter==="All"||q.source===sourceFilter)} store={store} pickup={(id)=>{const current=store.quotes.find(q=>q.id===id);if(!current||current.assignedTo){notify("Request was already assigned. Pool refreshed.");return}const proposal=deriveClassification(current,store.opportunities.find(o=>o.id===current.opportunityId));update(id,{status:"Assigned",assignedTo:"Yamin Shaikh",pickupSlaMinutes:0,systemClassification:proposal.classification,classificationTriggers:proposal.triggers},`${id} picked up by Yamin Shaikh; system proposed ${classificationLabel(proposal.classification)}`);setSelectedRequest(id)}}/></Panel>{selected&&<AssignedRequestDetail q={selected} store={store} update={update} close={()=>setSelectedRequest(null)} prepare={()=>setPrepareRequest(selected.id)}/>}</>;if(section==="My Assigned Requests")return <><Panel title="My Assigned Requests"><QuoteWork rows={assigned} update={update} onOpen={setSelectedRequest}/></Panel>{selected&&<AssignedRequestDetail q={selected} store={store} update={update} close={()=>setSelectedRequest(null)} prepare={()=>setPrepareRequest(selected.id)}/>}</>;if(section==="Quotations")return <Panel title="Quotations"><QuoteWork rows={store.quotes.filter(q=>q.classification)} update={update} onOpen={setSelectedRequest}/></Panel>;if(section==="Orders")return <IstOrderQueue store={store} setStore={setStore} notify={notify}/>;return <Placeholder section={section}/>}

function QuoteWork({rows,update,onOpen}:{rows:QuoteRequest[];update:(id:string,p:Partial<QuoteRequest>,m:string)=>void;onOpen?:(id:string)=>void}){return <div>{rows.map(q=><div className="work" key={q.id}><div><b>{q.id}</b><span>{q.products} · Qty {q.quantity}</span><small>{q.source} · {q.status}{q.classification?` · ${q.classification==="Tender"?"Tender / Project":q.classification}`:" · Awaiting classification"}{q.blocked?` · BLOCKED: ${q.blocked}`:""}</small></div><div className="workActions"><button className={!q.classification?"primary":""} onClick={()=>onOpen?.(q.id)}>{q.classification?"Open":"Review & classify"}</button></div></div>)}</div>}

function QuotePoolTable({rows,store,pickup}:{rows:QuoteRequest[];store:Store;pickup:(id:string)=>void}){return <table><thead><tr><th>Request</th><th>Source</th><th>Reseller / Opportunity</th><th>Product</th><th>Received</th><th>Pickup SLA</th><th>Blocker</th><th>Action</th></tr></thead><tbody>{rows.map(q=>{const r=store.resellers.find(x=>x.id===q.resellerId);return <tr key={q.id}><td><b>{q.id}</b><small>Unassigned</small></td><td>{q.source}</td><td>{r?.name||q.resellerId}<small>{q.opportunityId}</small></td><td>{q.products}<small>Qty {q.quantity}</small></td><td>{new Date(q.receivedAt).toLocaleString()}</td><td><Status s={q.pickupSlaMinutes<0?`Breached +${Math.abs(q.pickupSlaMinutes)} min`:`${q.pickupSlaMinutes} min left`}/></td><td>{q.blocked||"None"}</td><td><button className="primary" onClick={()=>pickup(q.id)}>Pick up</button></td></tr>})}</tbody></table>}

function AssignedRequestDetail({q,store,update,close,prepare}:{q:QuoteRequest;store:Store;update:(id:string,p:Partial<QuoteRequest>,m:string)=>void;close:()=>void;prepare:()=>void}){const r=store.resellers.find(x=>x.id===q.resellerId);const o=store.opportunities.find(x=>x.id===q.opportunityId);const tenderTriggers=tenderClassificationTriggers;const complexTriggers=complexClassificationTriggers;const derived=deriveClassification(q,o);const [triggers,setTriggers]=useState<string[]>(q.classificationTriggers||derived.triggers);const proposed:QuoteRequest["classification"]=triggers.some(x=>tenderTriggers.includes(x))?"Tender":triggers.some(x=>complexTriggers.includes(x))?"Complex":"Standard";const [choice,setChoice]=useState<QuoteRequest["classification"]>(q.classification||proposed);const [overrideReason,setOverrideReason]=useState(q.classificationOverrideReason||"");const [targetDelivery,setTargetDelivery]=useState(q.targetDeliveryAt||"");const [targetReason,setTargetReason]=useState(q.targetDeliveryJustification||"");const [ownerConfirmed,setOwnerConfirmed]=useState(!!q.tenderOwnerConfirmed);const [responsePlan,setResponsePlan]=useState(q.tenderResponsePlan||"");const [submissionDeadline,setSubmissionDeadline]=useState(q.tenderSubmissionDeadline||"");const [reclassifying,setReclassifying]=useState(false);const toggle=(x:string)=>setTriggers(v=>{const next=v.includes(x)?v.filter(i=>i!==x):[...v,x];const nextProposal:QuoteRequest["classification"]=next.some(i=>tenderTriggers.includes(i))?"Tender":next.some(i=>complexTriggers.includes(i))?"Complex":"Standard";setChoice(nextProposal);setOverrideReason("");return next});const confirm=()=>{if((choice!==proposed||(q.classification&&q.classification!==choice))&&!overrideReason.trim()){alert("Enter a reason for overriding or changing the classification.");return}if(choice==="Complex"&&(!targetDelivery||!targetReason.trim())){alert("Complex Quotes require a target delivery date/time and justification.");return}if(choice==="Tender"&&(!ownerConfirmed||!responsePlan.trim()||!submissionDeadline)){alert("Tender / Project Quotes require confirmed ownership, a response plan and the customer submission deadline.");return}const now=Date.now();const changeReason=overrideReason.trim()||(q.classification&&q.classification!==choice?"Classification changed after scope review":"System proposal accepted");const history=q.classification&&q.classification!==choice?[...(q.classificationHistory||[]),{from:q.classification,to:choice!,reason:changeReason,changedBy:q.assignedTo||"IST",changedAt:new Date(now).toISOString()}]:q.classificationHistory;const patch:Partial<QuoteRequest>={classification:choice,systemClassification:proposed,classificationTriggers:triggers,classificationOverrideReason:choice!==proposed?overrideReason.trim():undefined,classifiedAt:new Date(now).toISOString(),classificationHistory:history,status:"In Progress"};if(choice==="Standard")Object.assign(patch,{slaModel:"Standard issuance",quoteSlaDueAt:new Date(now+2*60*60*1000).toISOString()});if(choice==="Complex")Object.assign(patch,{slaModel:"Complex qualification",qualificationDueAt:new Date(now+2*60*60*1000).toISOString(),targetDeliveryAt:targetDelivery,targetDeliveryJustification:targetReason.trim()});if(choice==="Tender")Object.assign(patch,{slaModel:"Tender governance",tenderOwnerConfirmed:true,tenderResponsePlan:responsePlan.trim(),tenderSubmissionDeadline:submissionDeadline});setReclassifying(false);update(q.id,patch,`${q.id} classified ${choice==="Tender"?"Tender / Project":choice}; ${choice==="Standard"?"2-hour issuance SLA":choice==="Complex"?"2-hour qualification checkpoint":"tender governance tracking"} started`)};const slaSummary=q.classification==="Standard"?["2-hour quotation issuance SLA",q.quoteSlaDueAt?`Due: ${new Date(q.quoteSlaDueAt).toLocaleString()}`:"Due time recorded at classification"]:q.classification==="Complex"?["Qualification checkpoint: 2 hours",q.qualificationDueAt?`Qualification due: ${new Date(q.qualificationDueAt).toLocaleString()}`:"",q.targetDeliveryAt?`Committed delivery: ${new Date(q.targetDeliveryAt).toLocaleString()}`:"",q.targetDeliveryJustification?`Basis: ${q.targetDeliveryJustification}`:""]:q.classification==="Tender"?["No 2-hour quote-issuance SLA",q.tenderOwnerConfirmed?`Owner confirmed: ${q.assignedTo}`:"Owner confirmation required",q.tenderSubmissionDeadline?`Customer deadline: ${new Date(q.tenderSubmissionDeadline).toLocaleString()}`:"",q.tenderResponsePlan?`Response plan: ${q.tenderResponsePlan}`:""]:[];return <Panel title={`${q.id} · Assigned Request Details`}><div className="pageActions"><button onClick={close}>Back to list</button><Status s={q.status}/></div><div className="detailGrid expanded"><Info t="Request" lines={[`Source: ${q.source}`,`Owner: ${q.assignedTo||"Unassigned"}`,`Pickup SLA: stopped`]}/><Info t="Reseller" lines={[r?.name||"—",r?.status||"—",r?.country||"—"]}/><Info t="Opportunity" lines={[o?.id||"—",o?.name||"—",o?.stage||"—"]}/><Info t="Requirement" lines={[q.products,`Quantity: ${q.quantity}`,q.blocked?`Blocker: ${q.blocked}`:"No blocker"]}/></div>{(!q.classification||reclassifying)?<><div className="notice"><b>Classification review</b><br/>Select every condition that applies. The system evaluates Tender / Project first, then Complex; Standard applies only when neither category is triggered.</div><div className="classificationGrid"><fieldset><legend>Tender / Project triggers</legend>{tenderTriggers.map(x=><label key={x}><input type="checkbox" checked={triggers.includes(x)} onChange={()=>toggle(x)}/>{x}</label>)}</fieldset><fieldset><legend>Complex triggers</legend>{complexTriggers.map(x=><label key={x}><input type="checkbox" checked={triggers.includes(x)} onChange={()=>toggle(x)}/>{x}</label>)}</fieldset></div><div className="form classificationDecision"><label>System proposal<input value={proposed==="Tender"?"Tender / Project":proposed} readOnly/></label><label>Final classification *<select value={choice} onChange={e=>setChoice(e.target.value as any)}><option value="Standard">Standard Quote</option><option value="Complex">Complex Quote</option><option value="Tender">Tender / Project Quote</option></select></label>{(choice!==proposed||(q.classification&&q.classification!==choice))&&<label className="full">Override / reclassification reason *<textarea value={overrideReason} onChange={e=>setOverrideReason(e.target.value)} placeholder="Explain why business judgement differs from the system proposal"/></label>}{choice==="Standard"&&<div className="full slaBox"><b>Standard SLA</b><span>Quotation must be issued within 2 hours after classification.</span></div>}{choice==="Complex"&&<><div className="full slaBox"><b>Complex SLA</b><span>Qualification must be completed within 2 hours. Quote issuance follows the justified delivery commitment below.</span></div><label>Target delivery date & time *<input type="datetime-local" value={targetDelivery} onChange={e=>setTargetDelivery(e.target.value)}/></label><label>Delivery-target justification *<textarea value={targetReason} onChange={e=>setTargetReason(e.target.value)} placeholder="Dependencies, technical sizing, vendor pricing, multi-BU coordination…"/></label></>}{choice==="Tender"&&<><div className="full slaBox"><b>Tender / Project governance</b><span>No 2-hour issuance SLA. Track confirmed ownership, the response plan and the customer deadline.</span></div><label className="checkboxLabel"><input type="checkbox" checked={ownerConfirmed} onChange={e=>setOwnerConfirmed(e.target.checked)}/> I confirm ownership of this Tender / Project response *</label><label>Customer submission deadline *<input type="datetime-local" value={submissionDeadline} onChange={e=>setSubmissionDeadline(e.target.value)}/></label><label className="full">Response plan *<textarea value={responsePlan} onChange={e=>setResponsePlan(e.target.value)} placeholder="Owners, workstreams, internal milestones, vendor dependencies and review plan"/></label></>}<p className="full hint"><b>Audit:</b> system proposal, triggers, final classification, SLA commitment, IST user, timestamp and any override reason are retained.</p><div className="formActions full"><button onClick={close}>Cancel</button><button className="primary" onClick={confirm}>Confirm Classification & Activate SLA</button></div></div></>:<><div className="notice"><b>{q.classification==="Tender"?"Tender / Project":q.classification} Quote · {q.slaModel}</b><br/>{slaSummary.filter(Boolean).map((x,i)=><span key={i}>{x}<br/></span>)}<small>System proposal: {q.systemClassification==="Tender"?"Tender / Project":q.systemClassification||q.classification}{q.classificationOverrideReason?` · Override: ${q.classificationOverrideReason}`:""}</small></div><div className="pageActions"><button onClick={()=>setReclassifying(true)}>Reclassify</button><button className="primary" onClick={prepare}>Prepare Quotation</button></div>{q.classificationHistory?.length?<div className="notice"><b>Reclassification history</b>{q.classificationHistory.map((h,i)=><small key={i}>{classificationLabel(h.from)} → {classificationLabel(h.to)} · {h.reason} · {h.changedBy} · {new Date(h.changedAt).toLocaleString()}</small>)}</div>:null}</>}</Panel>}

function QuotationPreparation({q,store,update,back,notify}:{q:QuoteRequest;store:Store;update:(id:string,p:Partial<QuoteRequest>,m:string)=>void;back:()=>void;notify:(s:string)=>void}){const reseller=store.resellers.find(r=>r.id===q.resellerId);const opportunity=store.opportunities.find(o=>o.id===q.opportunityId);const [quantity,setQuantity]=useState(q.quantity);const [stock,setStock]=useState(Math.max(q.quantity,15));const [aitekPrice,setAitekPrice]=useState(105000);const [vendorPrice,setVendorPrice]=useState(65855.1);const [discountPercent,setDiscountPercent]=useState(10);const [currency,setCurrency]=useState(opportunity?.currency||"XOF");const [tva,setTva]=useState(true);const [airsi,setAirsi]=useState(true);const [delivery,setDelivery]=useState("Standard delivery conditions");const converted=aitekPrice*(1-discountPercent/100);const subtotal=converted*quantity;const tvaAmount=tva?subtotal*.18:0;const airsiAmount=airsi?subtotal*.055:0;const total=subtotal+tvaAmount+airsiAmount;const publish=()=>{if(q.blocked){notify(`Publication blocked: ${q.blocked}`);return}if(!quantity||!aitekPrice||!delivery.trim()){notify("Complete quantity, AITEK Price and Delivery Conditions");return}update(q.id,{status:"Published",version:q.version+1},`${q.id} quotation v${q.version+1} published; reseller and assigned KAM notified`);back()};return <><Panel title={`Prepare Quotation · ${q.id}`}><div className="pageActions"><Status s={`${q.classification} · SLA running`}/><button onClick={back}>Back to Request</button><button onClick={()=>notify("Quotation draft saved")}>Save Draft</button><button className="primary" onClick={publish}>Send to Reseller</button></div><div className="detailGrid expanded"><Info t="Traceability" lines={[`Quote Request: ${q.id}`,`Opportunity: ${q.opportunityId}`,`Source: ${q.source}`]}/><Info t="Reseller" lines={[reseller?.name||"—",reseller?.country||"—",reseller?.status||"—"]}/><Info t="Ownership and SLA" lines={[`Assigned IST: ${q.assignedTo}`,`Classification: ${q.classification==="Tender"?"Tender / Project":q.classification}`,q.classification==="Standard"?`Issue within 2 hours · Due: ${q.quoteSlaDueAt?new Date(q.quoteSlaDueAt).toLocaleString():"recorded at classification"}`:q.classification==="Complex"?`Qualification within 2 hours · Delivery: ${q.targetDeliveryAt?new Date(q.targetDeliveryAt).toLocaleString():"—"}`:`Governance SLA · Submission: ${q.tenderSubmissionDeadline?new Date(q.tenderSubmissionDeadline).toLocaleString():"—"}`]}/></div>{q.blocked&&<p className="hint"><b>Publication blocker:</b> {q.blocked}. Pricing may be prepared and saved, but the quotation cannot be sent until the blocker is cleared.</p>}</Panel><Panel title="Quotation Settings"><div className="form"><label>Currency *<select value={currency} onChange={e=>setCurrency(e.target.value)}><option>XOF</option><option>EUR</option><option>USD</option><option>MAD</option><option>AED</option></select></label><label>Taxes<div className="workActions"><label><input type="checkbox" checked={tva} onChange={e=>setTva(e.target.checked)}/> TVA</label><label><input type="checkbox" checked={airsi} onChange={e=>setAirsi(e.target.checked)}/> AIRSI</label></div></label></div></Panel><Panel title="Products and Pricing"><table><thead><tr><th>Vendor</th><th>Product</th><th>Stock availability</th><th>Quantity</th><th>AITEK Price</th><th>Vendor Price</th><th>Discount</th><th>Converted Unit Price</th><th>Subtotal</th></tr></thead><tbody><tr><td>{opportunity?.vendor||"Microsoft"}</td><td>{q.products}<small>SKU: linked existing product</small></td><td><input type="number" value={stock} onChange={e=>setStock(Number(e.target.value))}/>{stock<quantity&&<small className="danger">Requested quantity exceeds stock</small>}</td><td><input type="number" min="1" value={quantity} onChange={e=>setQuantity(Number(e.target.value))}/></td><td><input type="number" min="0" value={aitekPrice} onChange={e=>setAitekPrice(Number(e.target.value))}/></td><td><input type="number" min="0" value={vendorPrice} onChange={e=>setVendorPrice(Number(e.target.value))}/></td><td><input type="number" min="0" max="100" value={discountPercent} onChange={e=>setDiscountPercent(Number(e.target.value))}/><small>{(aitekPrice*discountPercent/100).toLocaleString()} discount</small></td><td>{currency} {converted.toLocaleString()}</td><td>{currency} {subtotal.toLocaleString()}</td></tr></tbody></table><div className="pageActions"><button>Add Product</button></div></Panel><Panel title="Price Break-up from AITEK"><div className="detailGrid"><Info t="Subtotal (Converted)" lines={[`${currency} ${subtotal.toLocaleString()}`]}/><Info t="TVA" lines={[`${currency} ${tvaAmount.toLocaleString()}`]}/><Info t="AIRSI" lines={[`${currency} ${airsiAmount.toLocaleString()}`]}/><Info t="Total Converted Price" lines={[`${currency} ${total.toLocaleString()}`]}/></div><div className="form" style={{marginTop:14}}><label className="full">Delivery Conditions *<textarea value={delivery} onChange={e=>setDelivery(e.target.value)}/></label><label className="full">Internal / Reseller Comment<textarea placeholder="Optional commercial note"/></label></div><div className="pageActions"><button onClick={()=>notify("Quotation draft saved")}>Save Draft</button><button className="primary" disabled={!!q.blocked||stock<quantity} onClick={publish}>Send to Reseller</button></div></Panel></>}

function Desk({section,store,setStore,notify}:Common&{section:string}){
 const blocked=store.quotes.filter(q=>q.blocked);const unassigned=store.quotes.filter(q=>q.status==="In IST Pool");const classified=store.quotes.filter(q=>q.classification);const overrides=classified.filter(q=>q.classificationOverrideReason);
 const reassign=(id:string,to:string,reason:string)=>{const current=store.quotes.find(q=>q.id===id);if(!current||!to||!reason.trim())return;const event={from:current.assignedTo,to,reason:reason.trim(),changedBy:users.DESK_MANAGER.name,changedAt:new Date().toISOString()};setStore({...store,quotes:store.quotes.map(x=>x.id===id?{...x,assignedTo:to,status:x.status==="In IST Pool"?"Assigned":x.status,assignmentHistory:[...(x.assignmentHistory||[]),event]}:x),notifications:[`${id} operational assignment changed from ${current.assignedTo||"Unassigned"} to ${to} by Desk Manager`,...store.notifications]});notify(to===current.assignedTo?`${id} retained by ${to}; supervision reason recorded`:`${id} reassigned to ${to}; audit history recorded`)};
 if(section==="Dashboard")return <><div className="notice"><b>Desk Manager role: workflow supervision</b><br/>Monitors the shared desk, SLA compliance, prioritisation, blockers and IST workload. Reassignment changes only the operational IST owner. KAM ownership, client relationship ownership and revenue attribution remain unchanged.</div><div className="cards"><Card t="Unassigned Pool" v={unassigned.length}/><Card t="Pickup SLA Breached" v={store.quotes.filter(isSlaBreached).length}/><Card t="Standard / Complex / Tender" v={`${classified.filter(q=>q.classification==="Standard").length} / ${classified.filter(q=>q.classification==="Complex").length} / ${classified.filter(q=>q.classification==="Tender").length}`}/><Card t="IST Team Members" v={istTeam.length}/></div><Panel title="Classification, SLA and Assignment Governance"><ClassificationGovernance rows={store.quotes} store={store} reassign={reassign}/></Panel></>;
 if(section==="IST Pool"||section==="SLA Monitor"||section==="Blocked Requests")return <Panel title={section}><ClassificationGovernance rows={store.quotes.filter(q=>section!=="Blocked Requests"||q.blocked)} store={store} reassign={reassign}/></Panel>;
 if(section==="Team Workload")return <Panel title="Central IST Team Workload"><p className="hint">The Desk Manager may redistribute operational work across all five IST members. This does not transfer the commercial deal owner.</p><table><thead><tr><th>IST Member</th><th>Open Requests</th><th>SLA Breached</th><th>Standard</th><th>Complex</th><th>Tender / Project</th></tr></thead><tbody>{istTeam.map(name=>{const rows=store.quotes.filter(q=>q.assignedTo===name&&!['Published','Accepted'].includes(q.status));return <tr key={name}><td><b>{name}</b></td><td>{rows.length}</td><td>{rows.filter(isSlaBreached).length}</td><td>{rows.filter(q=>q.classification==="Standard").length}</td><td>{rows.filter(q=>q.classification==="Complex").length}</td><td>{rows.filter(q=>q.classification==="Tender").length}</td></tr>})}</tbody></table></Panel>;
 if(section==="Reports")return <><Panel title="Classification Reporting"><div className="detailGrid expanded"><Info t="Classification Mix" lines={[`Standard: ${classified.filter(q=>q.classification==="Standard").length}`,`Complex: ${classified.filter(q=>q.classification==="Complex").length}`,`Tender / Project: ${classified.filter(q=>q.classification==="Tender").length}`]}/><Info t="Automation Quality" lines={[`System proposals accepted: ${classified.length-overrides.length}`,`Manual overrides: ${overrides.length}`,`Override rate: ${classified.length?Math.round(overrides.length/classified.length*100):0}%`]}/><Info t="Source Mix" lines={[`KAM: ${store.quotes.filter(q=>q.source==="KAM").length}`,`AITEKCenter: ${store.quotes.filter(q=>q.source==="AITEKCenter").length}`,`Outlook / Email: ${store.quotes.filter(q=>q.source==="Outlook / Email").length}`]}/></div></Panel><Panel title="Override Reasons"><table><thead><tr><th>Request</th><th>System Proposal</th><th>Final</th><th>Reason</th><th>Classified By / At</th></tr></thead><tbody>{overrides.map(q=><tr key={q.id}><td>{q.id}</td><td>{classificationLabel(q.systemClassification)}</td><td>{classificationLabel(q.classification)}</td><td>{q.classificationOverrideReason}</td><td>{q.assignedTo}<small>{q.classifiedAt?new Date(q.classifiedAt).toLocaleString():"—"}</small></td></tr>)}</tbody></table></Panel></>;
 return <Placeholder section={section}/>;
}
function ClassificationGovernance({rows,store,reassign}:{rows:QuoteRequest[];store:Store;reassign:(id:string,to:string,reason:string)=>void}){return <table><thead><tr><th>Received / Quote</th><th>Source / Client</th><th>BU / Vendor / Value</th><th>Operational IST Owner</th><th>Classification / Trigger</th><th>SLA / Status</th><th>Desk Manager Action</th></tr></thead><tbody>{rows.map(q=>{const o=store.opportunities.find(x=>x.id===q.opportunityId);const r=store.resellers.find(x=>x.id===q.resellerId);return <tr key={q.id}><td><b>{q.receivedAt?new Date(q.receivedAt).toLocaleString():"—"}</b><small>{q.id} · v{q.version}</small></td><td>{q.source}<small>{r?.name||q.resellerId}</small></td><td>{o?.businessUnit||"To confirm"}<small>{o?.vendor||"To confirm"} · {money(o?.amount)}</small></td><td>{q.assignedTo||"Unassigned"}<small>{q.assignmentHistory?.length?`${q.assignmentHistory.length} assignment change(s) recorded`:"No reassignment history"}</small></td><td>{classificationLabel(q.classification)}<small>{q.classificationTriggers?.[0]||"Awaiting IST classification"}</small></td><td>{q.slaModel||"Pickup SLA"}<small>{quoteSlaLabel(q)}</small><Status s={isSlaBreached(q)?"SLA Breached":q.status}/>{q.blocked&&<small className="danger">{q.blocked}</small>}</td><td><AssignmentControl q={q} reassign={reassign}/></td></tr>})}</tbody></table>}

function AssignmentControl({q,reassign}:{q:QuoteRequest;reassign:(id:string,to:string,reason:string)=>void}){const [open,setOpen]=useState(false);const [to,setTo]=useState(q.assignedTo||istTeam[0]);const [reason,setReason]=useState("");const save=()=>{if(!reason.trim()){alert("A reassignment or retention reason is mandatory for audit.");return}reassign(q.id,to,reason);setReason("");setOpen(false)};if(!open)return <button className={isSlaBreached(q)?"primary":""} onClick={()=>setOpen(true)}>{q.assignedTo?"Review Assignment":"Assign IST"}</button>;return <div className="assignmentControl"><select value={to} onChange={e=>setTo(e.target.value)}>{istTeam.map(name=><option key={name}>{name}</option>)}</select><textarea value={reason} onChange={e=>setReason(e.target.value)} placeholder="Mandatory reason: SLA breach, workload balancing, absence, specialist skill…"/><div><button onClick={()=>setOpen(false)}>Cancel</button><button className="primary" onClick={save}>{to===q.assignedTo?"Keep & Record":"Reassign"}</button></div><small>Commercial owner and KAM attribution will not change.</small></div>}

function IstOrderQueue({store,setStore,notify}:Common){const [active,setActive]=useState<string|null>(null);const q=store.quotes.find(x=>x.id===active);const placeOrder=(id:string,reason:string,authority:string)=>{if(!reason.trim()||!authority.trim()){alert("Reason and authority source are required.");return}const current=store.quotes.find(x=>x.id===id);setStore({...store,quotes:store.quotes.map(x=>x.id===id?{...x,status:"Order Placed",orderOnBehalf:{placedBy:users.IST.name,placedByRole:"IST",reason:reason.trim(),authoritySource:authority.trim(),placedAt:new Date().toISOString()}}:x),opportunities:store.opportunities.map(o=>o.id===current?.opportunityId?{...o,stage:"Won",probability:100,weightedPipeline:o.amount||0,nextAction:"Finance payment validation",nextActionDescription:"Payment proof submitted with offline acceptance. Finance to validate before invoice/payment closure."}:o),notifications:[`${id}: order placed on behalf of reseller by ${users.IST.name}; payment proof pending Finance validation`,...store.notifications]});notify("Order placed on behalf. Payment proof moved to Finance validation.");setActive(null)};return <Panel title="Orders - Offline Acceptance Handoffs"><p className="hint">This queue contains strategic reseller cases where KAM received offline acceptance/payment proof. IST performs the official order placement on behalf of the reseller and captures authority evidence.</p><table><thead><tr><th>Quote</th><th>Reseller / Opportunity</th><th>Product</th><th>Payment Proof</th><th>KAM Handoff</th><th>Status</th><th>Action</th></tr></thead><tbody>{store.quotes.filter(x=>x.offlineAcceptance).map(x=>{const r=store.resellers.find(res=>res.id===x.resellerId);return <tr key={x.id}><td><b>{x.id}</b><small>v{x.version}</small></td><td>{r?.name}<small>{x.opportunityId}</small></td><td>{x.products}</td><td>{x.offlineAcceptance?.proofFileName}<small>{x.offlineAcceptance?.paymentReference} · {x.offlineAcceptance?.amount}</small></td><td>{x.offlineAcceptance?.recordedBy}<small>{x.offlineAcceptance?.confirmationMode}</small></td><td><Status s={x.orderOnBehalf?"Order Placed":"Pending IST Order Placement"}/></td><td>{x.orderOnBehalf?<small>{x.orderOnBehalf.authoritySource}</small>:<button className="primary" onClick={()=>setActive(x.id)}>Place Order on Behalf</button>}</td></tr>})}</tbody></table>{q&&<OrderOnBehalfModal q={q} store={store} close={()=>setActive(null)} placeOrder={placeOrder}/>}</Panel>}

function OrderOnBehalfModal({q,store,close,placeOrder}:{q:QuoteRequest;store:Store;close:()=>void;placeOrder:(id:string,reason:string,authority:string)=>void}){const reseller=store.resellers.find(r=>r.id===q.resellerId);const [reason,setReason]=useState("Strategic reseller paid offline and sent proof to KAM.");const [authority,setAuthority]=useState("Payment proof and KAM confirmation attached");return <Modal title={`Place Order on Behalf · ${q.id}`} close={close}><div className="form"><div className="full formSection"><b>Authorised IST action</b><span>This creates the operational order on behalf of the reseller. It does not change KAM commercial ownership.</span></div><label>Reseller<input value={reseller?.name||""} readOnly/></label><label>Proof Received<input value={q.offlineAcceptance?.proofFileName||""} readOnly/></label><label>Payment Reference<input value={q.offlineAcceptance?.paymentReference||""} readOnly/></label><label>Amount<input value={q.offlineAcceptance?.amount||""} readOnly/></label><label className="full">Reason *<textarea value={reason} onChange={e=>setReason(e.target.value)}/></label><label className="full">Authority Source *<textarea value={authority} onChange={e=>setAuthority(e.target.value)} placeholder="Email approval, PO, payment receipt, KAM confirmation..."/></label><p className="full hint"><b>System action:</b> order status becomes Order Placed, payment proof goes to Finance validation, and KAM remains the commercial owner.</p><div className="formActions full"><button onClick={close}>Cancel</button><button className="primary" onClick={()=>placeOrder(q.id,reason,authority)}>Confirm Order Placement</button></div></div></Modal>}

function Regional({section,store}:{section:string;store:Store}){const regional=store.opportunities.filter(o=>o.regionalDirector===users.REGIONAL_MANAGER.name);const active=regional.filter(o=>o.status==="Active");const lost=regional.filter(o=>o.status==="Lost");const regionalIds=new Set(regional.map(o=>o.id));const regionalQuotes=store.quotes.filter(q=>regionalIds.has(q.opportunityId));const inactive30=active.filter(o=>(daysFrom(o.lastActivityDate)||999)>30);const missing=active.filter(actionMissing);const late=active.filter(o=>delayStatus(o)==="Late");const activities=store.activities.filter(a=>regionalIds.has(a.opportunityId));return <><div className="cards"><Card t="Regional Pipeline" v={money(active.reduce((a,o)=>a+(o.amount||0),0))}/><Card t="Weighted Pipeline" v={money(active.reduce((a,o)=>a+o.weightedPipeline,0))}/><Card t="No Activity >30d" v={inactive30.length}/><Card t="Missing Next Action" v={missing.length}/><Card t="Late Deals" v={late.length}/><Card t="Client Interactions" v={activities.length}/></div><Panel title={section}><div className="charts"><div><h3>Pipeline by stage</h3>{stages.map(s=><div className="bar" key={s}><span>{s}</span><i style={{width:`${20+regional.filter(o=>o.stage===s).length*45}%`}}/></div>)}</div><div><h3>KAM performance by region</h3><table><thead><tr><th>KAM</th><th>Active Deals</th><th>Pipeline</th><th>Weighted</th><th>No Activity &gt;30d</th><th>Missing Action</th><th>Late</th><th>Activities</th></tr></thead><tbody><tr><td>Aminata Koné<small>Reports to Koffi Traoré · DR CI & WEST</small></td><td>{active.length}</td><td>{money(active.reduce((a,o)=>a+(o.amount||0),0))}</td><td>{money(active.reduce((a,o)=>a+o.weightedPipeline,0))}</td><td>{inactive30.length}</td><td>{missing.length}</td><td>{late.length}</td><td>{activities.length}</td></tr></tbody></table><h3>Won / Lost</h3><p>{regional.filter(o=>o.status==="Won").length} Won · {lost.length} Lost · {regionalQuotes.filter(q=>q.status==="Published").length} published quotes</p></div></div></Panel></>}

function OpportunityTable({rows,onOpen}:{rows:Opportunity[];onOpen:(id:string)=>void}){return <table><thead><tr><th>Opportunity</th><th>Source / Type</th><th>BU / Vendor</th><th>Stage</th><th>Amount / Weighted</th><th>Last Activity</th><th>Next Action</th><th>Risk / Delay</th><th></th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td><b>{o.name}</b><small>{o.id}</small></td><td>{o.source}<small>{o.opportunityType}</small></td><td>{o.businessUnit||"—"}<small>{o.vendor||"—"}</small></td><td>{o.stage||"Draft"}<small>{o.probability||0}% probability</small></td><td>{money(o.amount)}<small>Weighted: {money(o.weightedPipeline)}</small></td><td>{o.lastActivityDate||"—"}<small>{daysFrom(o.lastActivityDate)??"—"} days inactive</small></td><td>{o.nextAction||"—"}<small>{o.nextDate||"No date"} {actionMissing(o)?"· missing":""}</small></td><td>{o.risk}<small>{delayStatus(o)}</small></td><td><button onClick={()=>onOpen(o.id)}>Open</button></td></tr>)}</tbody></table>}
function KamQuoteTable({store,rows,onOffline}:{store:Store;rows:QuoteRequest[];onOffline:(id:string)=>void}){return <Panel title="Quote Requests & Quotations"><table><thead><tr><th>Request</th><th>Opportunity</th><th>Product</th><th>Classification</th><th>SLA commitment</th><th>Status</th><th>Version</th><th>KAM Action</th></tr></thead><tbody>{rows.map(q=><tr key={q.id}><td><b>{q.id}</b>{q.blocked&&<small className="danger">Blocked: {q.blocked}</small>}</td><td>{q.opportunityId}</td><td>{q.products}</td><td>{classificationLabel(q.classification)}<small>{q.classification?`System proposed: ${classificationLabel(q.systemClassification||q.classification)}`:"IST will classify after pickup"}</small></td><td>{quoteSlaLabel(q)}</td><td><Status s={q.orderOnBehalf?"Order Placed":q.offlineAcceptance?"Offline Payment Recorded":q.status}/></td><td>v{q.version}</td><td>{q.status==="Published"&&!q.offlineAcceptance?<button className="primary" onClick={()=>onOffline(q.id)}>Record Offline Acceptance / Payment Proof</button>:q.offlineAcceptance?<small>Sent to IST for order placement</small>:<small>Available after quotation is published</small>}</td></tr>)}</tbody></table></Panel>}
function OfflinePaymentForm({q,store,setStore,close,notify}:Common&{q:QuoteRequest;close:()=>void}){const reseller=store.resellers.find(r=>r.id===q.resellerId);const opportunity=store.opportunities.find(o=>o.id===q.opportunityId);const [mode,setMode]=useState("Email confirmation");const [reference,setReference]=useState("");const [proof,setProof]=useState("");const [amount,setAmount]=useState("");const [notes,setNotes]=useState("Strategic reseller confirmed offline after KAM negotiation.");const submit=(e:FormEvent)=>{e.preventDefault();if(!reference.trim()||!proof.trim()||!amount.trim()){notify("Payment reference, proof file name and amount are required.");return}const offlineAcceptance={recordedBy:users.KAM.name,recordedByRole:"KAM" as const,receivedFrom:reseller?.name||"Strategic reseller",confirmationMode:mode,paymentReference:reference.trim(),proofFileName:proof.trim(),amount:amount.trim(),notes:notes.trim(),recordedAt:new Date().toISOString()};setStore({...store,quotes:store.quotes.map(x=>x.id===q.id?{...x,offlineAcceptance,status:"Payment Proof Received",assignedTo:x.assignedTo||"Yamin Shaikh"}:x),opportunities:store.opportunities.map(o=>o.id===q.opportunityId?{...o,stage:"Won",probability:100,weightedPipeline:o.amount||0,nextAction:"Prepare order",nextActionDescription:"IST to place order on behalf after offline payment proof received"}:o),notifications:[`${q.id}: ${reseller?.name||"Strategic reseller"} paid offline and sent proof to KAM; IST must place order on behalf`,...store.notifications]});notify("Offline acceptance and payment proof recorded. IST has been notified to place order on behalf.");close()};return <Modal title={`Record Offline Acceptance / Payment Proof · ${q.id}`} close={close}><form onSubmit={submit} className="form"><div className="full formSection"><b>KAM commercial handoff</b><span>KAM records proof received from the strategic reseller. KAM does not place the order. Authorised IST or Desk Manager completes order placement on behalf of the reseller.</span></div><label>Strategic Reseller<input value={reseller?.name||""} readOnly/></label><label>Opportunity<input value={`${opportunity?.id||q.opportunityId} · ${opportunity?.name||""}`} readOnly/></label><label>Confirmation Mode *<select value={mode} onChange={e=>setMode(e.target.value)}><option>Email confirmation</option><option>WhatsApp confirmation</option><option>Phone confirmation</option><option>Purchase Order received</option><option>Bank transfer proof received</option></select></label><label>Payment Reference / Transaction ID *<input value={reference} onChange={e=>setReference(e.target.value)} placeholder="Bank reference, transfer ID or PO number"/></label><label>Amount Paid *<input value={amount} onChange={e=>setAmount(e.target.value)} placeholder="Example: EUR 180,000"/></label><label>Proof File Name *<input value={proof} onChange={e=>setProof(e.target.value)} placeholder="Example: payment-proof-abc-tech.pdf"/></label><label className="full">KAM Notes *<textarea value={notes} onChange={e=>setNotes(e.target.value)}/></label><p className="full hint"><b>System action:</b> status becomes Payment Proof Received, opportunity moves to Won, and IST receives an order-placement task. Finance validation happens after order/payment proof is processed.</p><div className="formActions full"><button type="button" onClick={close}>Cancel</button><button className="primary">Submit to IST</button></div></form></Modal>}
function QuoteTable({store,rows}:{store:Store;rows?:QuoteRequest[]}){const visible=rows||store.quotes;return <Panel title="Quote Requests & Quotations"><table><thead><tr><th>Request</th><th>Opportunity</th><th>Product</th><th>Classification</th><th>Reason / Trigger</th><th>SLA commitment</th><th>Status</th><th>Version</th></tr></thead><tbody>{visible.map(q=><tr key={q.id}><td><b>{q.id}</b>{q.blocked&&<small className="danger">Blocked: {q.blocked}</small>}</td><td>{q.opportunityId}</td><td>{q.products}</td><td>{classificationLabel(q.classification)}<small>{q.classification?`System proposed: ${classificationLabel(q.systemClassification||q.classification)}`:"IST will classify after pickup"}</small></td><td>{q.classificationTriggers?.[0]||"—"}<small>{q.classificationOverrideReason?`Manual override: ${q.classificationOverrideReason}`:q.classification?"System proposal accepted":""}</small></td><td>{quoteSlaLabel(q)}</td><td><Status s={q.status}/></td><td>v{q.version}</td></tr>)}</tbody></table></Panel>}
function Card({t,v}:{t:string;v:string|number}){return <div className="card"><span>{t}</span><b>{v}</b></div>};function Status({s}:{s:string}){return <span className={`status ${s.toLowerCase().replaceAll(" ","-")}`}>{s}</span>};function Panel({title,children}:{title:string;children:any}){return <section className="panel"><div className="panelTitle"><h2>{title}</h2></div>{children}</section>};function Modal({title,close,children}:{title:string;close:()=>void;children:any}){return <div className="overlay"><div className="modal"><div className="modalHead"><h2>{title}</h2><button onClick={close}>×</button></div>{children}</div></div>};function Placeholder({section}:{section:string}){return <Panel title={section}><div className="empty"><b>{section}</b><p>This module is available in the navigation and uses the same shared role and permission context. Detailed production integration follows the approved PRD.</p></div></Panel>}
function Info({t,lines}:{t:string;lines:(string|undefined)[]}){return <div><small>{t}</small>{lines.filter(Boolean).map((x,i)=><b key={i}>{x}</b>)}</div>}



